import { NameFormartingPipe } from './name-formarting-pipe';

describe('NameFormartingPipe', () => {
  it('create an instance', () => {
    const pipe = new NameFormartingPipe();
    expect(pipe).toBeTruthy();
  });
});
